import { Coordinates } from '../types/game';
export function haversineDistanceMeters(a: Coordinates, b: Coordinates) { const R=6371000; const dLat=(b.latitude-a.latitude)*Math.PI/180; const dLon=(b.longitude-a.longitude)*Math.PI/180; const lat1=a.latitude*Math.PI/180; const lat2=b.latitude*Math.PI/180; const h=Math.sin(dLat/2)**2+Math.cos(lat1)*Math.cos(lat2)*Math.sin(dLon/2)**2; return Math.round(2*R*Math.asin(Math.sqrt(h))); }
export function randomCoordinateWithinRadius(center: Coordinates, radiusMeters: number): Coordinates { const r=radiusMeters*Math.sqrt(Math.random()); const theta=Math.random()*2*Math.PI; const dx=r*Math.cos(theta); const dy=r*Math.sin(theta); return { latitude:center.latitude + dy/111320, longitude:center.longitude + dx/(111320*Math.cos(center.latitude*Math.PI/180)) }; }
