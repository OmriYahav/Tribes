import { View } from 'react-native';
import { Body, FactionBadge, FactionBar, HUDCard, Screen, ScreenHeader } from '../components/ui';
import { buildLeaderboard } from '../services/mockPlayerService';
import { useGameStore } from '../store/gameStore';
import { colors, space } from '../theme/theme';
export function LeaderboardScreen() { const user = useGameStore(s => s.user); const score = useGameStore(s => s.stats.campContributionScore); const rows = buildLeaderboard(user, score); const enl = rows.filter(r => r.tribe === 'vanguard').reduce((a, r) => a + r.totalXp, 0); const res = rows.filter(r => r.tribe === 'cipher').reduce((a, r) => a + r.totalXp, 0); return <Screen><ScreenHeader title="Intel Dashboard" kicker="Live faction standings"/><HUDCard><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Body>ENL {enl}</Body><Body>RES {res}</Body></View><FactionBar left={enl} right={res}/></HUDCard><HUDCard><Body>REGIONAL RANKINGS</Body>{rows.slice(0, 3).map((r, i) => <HUDCard key={r.id} style={{ borderColor: i === 0 ? colors.amber : colors.line }}><View style={{ flexDirection: 'row', alignItems: 'center', gap: space.sm }}><Body>#{i + 1}</Body><View style={{ flex: 1 }}><Body>{r.username}{r.isCurrentUser ? ' // YOU' : ''}</Body><Body>L{r.level} • {r.totalXp} AP</Body></View><FactionBadge faction={r.tribe}/></View></HUDCard>)}</HUDCard><HUDCard><Body>GLOBAL RANKINGS</Body>{rows.map((r, i) => <View key={r.id} style={{ flexDirection: 'row', alignItems: 'center', gap: space.sm, paddingVertical: space.sm, borderBottomWidth: 1, borderBottomColor: colors.line }}><Body>#{i + 1}</Body><View style={{ flex: 1 }}><Body>{r.username}{r.isCurrentUser ? ' // YOU' : ''}</Body><Body>{r.contributionScore} CONTRIB • {r.missions} MISSIONS</Body></View><FactionBadge faction={r.tribe}/></View>)}</HUDCard></Screen>; }
